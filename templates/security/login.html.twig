<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Connexion - FlowForge</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="min-h-screen bg-gray-100 flex items-center justify-center py-8">
    <div class="w-full max-w-md px-4">
        <div class="bg-white p-8 rounded-lg shadow-lg">
            <div class="flex justify-center mb-6">
                <img src="{{ asset('images/logo-full.png') }}" alt="FlowForge" class="h-40">
            </div>

            {% if error %}
                <div class="bg-red-100 text-red-700 p-3 rounded-lg mb-4">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}

            <form method="post" class="space-y-4">
                <div>
                    <label for="username" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                    <input type="email" name="_username" id="username" value="{{ last_username }}" required autofocus
                        class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Mot de passe</label>
                    <input type="password" name="_password" id="password" required
                        class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                <button type="submit" class="w-full bg-indigo-600 text-white px-6 py-2 rounded-lg hover:bg-indigo-700 cursor-pointer">
                    Se connecter
                </button>
            </form>
        </div>

        <div class="bg-indigo-50 border border-indigo-200 p-6 rounded-lg shadow-lg mt-6">
            <h3 class="text-lg font-semibold text-indigo-800 mb-3 flex items-center">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                Comptes de démonstration
            </h3>
            <p class="text-sm text-indigo-600 mb-4">Utilisez ces identifiants pour tester l'application :</p>
            
            <div class="space-y-3">
                <div class="bg-white p-3 rounded border border-indigo-100">
                    <div class="flex justify-between items-center">
                        <span class="text-sm font-medium text-gray-700">Admin</span>
                        <span class="text-xs bg-red-100 text-red-700 px-2 py-1 rounded">ADMIN</span>
                    </div>
                    <p class="text-sm text-gray-600 mt-1">admin@flowforge.io</p>
                </div>

                <div class="bg-white p-3 rounded border border-indigo-100">
                    <div class="flex justify-between items-center">
                        <span class="text-sm font-medium text-gray-700">Manager</span>
                        <span class="text-xs bg-blue-100 text-blue-700 px-2 py-1 rounded">MANAGER</span>
                    </div>
                    <p class="text-sm text-gray-600 mt-1">sophie.bernard@company.fr</p>
                </div>

                <div class="bg-white p-3 rounded border border-indigo-100">
                    <div class="flex justify-between items-center">
                        <span class="text-sm font-medium text-gray-700">Utilisateur</span>
                        <span class="text-xs bg-green-100 text-green-700 px-2 py-1 rounded">USER</span>
                    </div>
                    <p class="text-sm text-gray-600 mt-1">marie.dupont@company.fr</p>
                </div>
            </div>

            <div class="mt-4 pt-3 border-t border-indigo-200">
                <p class="text-sm text-indigo-700">
                    <strong>Mot de passe :</strong> <code class="bg-white px-2 py-1 rounded text-indigo-800">password</code>
                </p>
            </div>

            <p class="text-xs text-indigo-500 mt-3">
                ⚠️ Les données sont réinitialisées chaque lundi.
            </p>
        </div>
    </div>
</div>
</body>
</html>